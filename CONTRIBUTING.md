# Contributing

Discuss large changes first. Preserve physical geometry, use patient-level data
splits, never commit PHI, document preprocessing, and add tests. Run:

```bash
ruff check src tests
pytest
```

