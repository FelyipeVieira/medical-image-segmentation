# Benchmark Template

Report dataset/version, patient-level split, preprocessing, spacing, model and
parameter count, patch/batch size, loss, seed count, hardware, precision,
training time, peak memory, per-volume latency, Dice/HD95/ASSD per class with
confidence intervals, and external-validation results.

